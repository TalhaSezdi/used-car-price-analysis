# Pinned to the exact versions that produced the reported metrics (Python 3.12).
pandas==2.3.3
numpy==2.4.4
scikit-learn==1.8.0
lightgbm==4.6.0
matplotlib==3.10.9
seaborn==0.13.2
scipy==1.17.1
pyarrow==23.0.1
tabulate==0.10.0
# Notebook execution stack (presentation.ipynb ships pre-executed with outputs;
# open it with any Jupyter frontend, e.g. VS Code or JupyterLab).
ipykernel==7.3.0
nbformat==5.10.4
nbclient==0.11.0
